

const divElement = document.createElement('div');
console.dir(divElement);

// divElement.setAttribute('id', attributeValue);
// divElement.setAttribute('title', attributeValue);
// divElement.setAttribute('class', attributeValue);
// divElement.setAttribute('aria-label', attributeValue);

const articleReactElement = React.createElement('article');

// DOM
// 실제 문서 객체 모델

// Virtual DOM
// 실제의 것을 추상화 한 것 
// 가상 문서 객체 모델

console.log(articleReactElement);